import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class TestSaveFileXML {
    @Test
    void shouldReturn(){
        // given (arrange)
        // well...
        // when (act)
        int result = 2*2;
        // then (assert)
        assertEquals(4, result);
    }

}
